{
  "version": 2,
  "buildCommand": "npm run build --workspace=lib && npm run build --workspace=client",
  "outputDirectory": "client/dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/route" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
